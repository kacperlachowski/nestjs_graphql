# table

type Table {
  id: Int!
  name: String!
  description: String
}

# schema

type Query {
  table: [Table]
}

type Mutation {
  addTable(name: String!, description: String): Table
}

type Subscription {
  newTable: Table
}
